# base의 web-config(기본값 dev)를 prod로 덮어쓰기
apiVersion: v1
kind: ConfigMap
metadata:
  name: web-config
  namespace: deep-quest
data:
  NODE_ENV: production
  NEXT_PUBLIC_SENTRY_ENV: production
  SENTRY_ALLOW_EMPTY_DSN: "false"
