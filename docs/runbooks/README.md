# App Runbooks

루트 애플리케이션 작업 영역에서 반복 실행하는 절차 문서는 이 디렉터리에 둔다.

문서를 추가하거나 수정할 때는 먼저 `../reference/document-writing-rules.md`를 따른다.

## 사용 원칙

- 재현 가능한 절차만 남긴다
- 실제 경로와 명령을 유지한다
- 장애 원인 분석 문구는 `../troubleshooting/`로 보낸다
- 비교표나 배경 설명은 `../reference/`로 보낸다
- 일회성 상태 기록은 `../status/`나 `../tasks/`에 남긴다
